const tratoP = require('../models/trado(pendiente).model');
const tratoC= require('../models/trato(Confirmado).model');


function Nuevotrato(req,res){
var userId = req.user.sub;


}
module.exports ={
    Nuevotrato
}